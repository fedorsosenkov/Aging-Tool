# Математические модели деградации МОП-транзисторов

Документ описывает все расчётные формулы, физические константы, допущения и
численные методы, применяемые в программе MOS Aging Analyzer.

---

## 1. Физические константы

| Обозначение | Значение | Единица | Смысл |
|---|---|---|---|
| k_B | 1.380649 × 10⁻²³ | Дж/К | Постоянная Больцмана |
| q | 1.60219 × 10⁻¹⁹ | Кл | Заряд электрона |
| ε₀ | 8.85 × 10⁻¹² | Ф/м | Диэлектрическая проницаемость вакуума |
| ε_SiO2 | 3.9 | — | Относительная проницаемость SiO₂ |
| t_year | 31 536 000 | с | Секунд в году |

Производные:

```
ε_ox = ε_SiO2 · ε₀  =  3.9 · 8.85×10⁻¹²  ≈  3.45×10⁻¹¹  Ф/м

Cox = ε_ox / tox                     [Ф/м²]  ёмкость окисла
```

---

## 2. Эффект горячих носителей (HCI) — NMOS-транзисторы

### 2.1 Физическая природа

При высоких полях вблизи стока электроны набирают достаточно энергии, чтобы
преодолеть барьер Si/SiO₂ и захватиться в окисел затвора. Это приводит к
сдвигу порогового напряжения Vth вверх и снижению подвижности носителей u0.

Интенсивность деградации определяется:
- пиковым значением тока стока Id,
- отношением тока горячих носителей к полному току (I_hci/Id),
- шириной канала W,
- временем работы в режиме переключения.

### 2.2 Ток горячих носителей (B-источник в .inc файле)

В LTSpice добавляется управляемый источник тока, аппроксимирующий поток
горячих носителей:

```
I_hci = 1×10⁻⁵ · Id(M) · (1/L_nm)^2.5
             · exp(5.7 · (V_D − V_S))
             · exp(−1.21 · (V_D − V_G))
```

Где:
- `L_nm` — длина канала в нанометрах (например, 100 для L=100нм)
- `V_D, V_S, V_G` — потенциалы стока, истока, затвора
- Множитель `(1/L_nm)^2.5` — масштабирование по длине канала
- `exp(5.7·(V_D−V_S))` — зависимость от поля вдоль канала
- `exp(−1.21·(V_D−V_G))` — зависимость от поля в окисле

LTSpice директивы, добавляемые в .inc для транзистора Mx с номером `num`:
```spice
BnumIb 0 NnumIb I=<формула выше>
RnumIb NnumIb 0 1
.measure tran HCI_Mx    integ I(BnumIb)
.measure tran maxHCI_Mx max   I(BnumIb)
.measure tran maxId_Mx  max   Id(Mx)
```

### 2.3 Извлечение измерений из Spice Error Log

После симуляции из `.log` файла берутся три значения:

| Переменная | Ключ в .log | Смысл |
|---|---|---|
| `integ_val` | `HCI_Mx` | ∫ I_hci dt  [Кл] |
| `max_hci` | `maxHCI_Mx` | max I_hci  [А] |
| `max_id` | `maxId_Mx` | max Id  [А] |

### 2.4 Время работы в режиме HCI

```
t_hci = |integ_val / (max_hci × 0.75)|      [с]
```

Коэффициент 0.75 — инженерное допущение: считается, что полная мощность
горячих носителей действует только 75 % времени переключения (учёт
неидеальности прямоугольника тока).

```
ratio_hci = |integ_val / (max_hci × 0.75 × t_sim)|   [безразм.]
```

Где `t_sim` — полное время симуляции (FROM…TO в .log).

### 2.5 Сдвиг порогового напряжения ΔVth (HCI)

Модель деградации по степенному закону:

```
ΔVth_HCI = 1.3 · C₂₂ · |Id_max/W · (max_hci/Id_max)^m · t_life · ratio|^n
```

Параметры модели:

| Параметр | Значение | Смысл |
|---|---|---|
| C₂₂ | 0.22 | эмпирический коэффициент калибровки |
| m | 2 | показатель степени по отношению токов |
| n | 0.27 | показатель степени временно́й зависимости |
| 1.3 | — | инженерный запас (margin) |
| t_life | years × 31 536 000 | требуемый ресурс [с] |

Размерный анализ аргумента:

```
[Id_max/W] = А/м
[(max_hci/Id_max)^m] = безразм.
[t_life · ratio] = с
→ аргумент: [А/м · с]  →  при n=0.27 размерность в В (эмпирически)
```

### 2.6 Коэффициент снижения подвижности (HCI)

```
factor_u0_HCI = 1 + (0.24×10⁻¹⁶) · (ΔVth_HCI · Cox / q)
```

Параметр u0 модели в netlist делится на `factor_u0`:

```
u0_aged = u0_original / factor_u0
```

---

## 3. Температурная нестабильность при отрицательном смещении (NBTI) — PMOS-транзисторы

### 3.1 Физическая природа

При отрицательном смещении затвора (режим инверсии PMOS) и повышенной
температуре ионы водорода из интерфейса Si/SiO₂ диффундируют в окисел.
Это создаёт положительный заряд, сдвигающий Vth в сторону более отрицательных
значений (|Vth| увеличивается) и снижающий подвижность дырок.

### 3.2 Директивы измерений в .inc файле

```spice
.measure tran maxNBTIMx max ((V(S)-V(G))*(V(D)-V(G)))
.measure tran NBTIMx    integ ABS(V(S)-V(G))
```

Где `V(D), V(G), V(S)` — потенциалы стока, затвора, истока.

### 3.3 Извлечение измерений

| Переменная | Ключ в .log | Смысл |
|---|---|---|
| `integ_val` | `NBTIMx` | ∫\|Vsg\| dt = ∫\|Vgs\| dt  [В·с] |
| `max_val` | `maxNBTIMx` | max(Vsg · Vdg)  [В²] |

### 3.4 Время работы в режиме NBTI

```
t_nbti = integ_val / max_val      [с]
ratio_nbti = t_nbti / t_sim       [безразм.]
```

Физический смысл: `integ / max` — это «эффективное время», в течение которого
транзистор работает при максимальных напряжениях, вызывающих NBTI.

### 3.5 Коэффициент Kv

Kv характеризует интенсивность генерации ловушек в зависимости от
электрического поля в окисле и температуры:

```
Kv = (tox · q / ε_ox)³
   · (1×10²⁶)²
   · Cox
   · (|Vgs_max| − Vth)
   · (exp(−E_a / (k_B · T)) / 10¹⁰)^0.5
   · exp(2 · (|Vgs_max| − Vth) / (tox · 0.355×10⁹))
```

Где:
- `Vgs_max = sqrt(max_val)` — эффективное максимальное напряжение затвор-исток
- `Vth` — пороговое напряжение PMOS-модели (задаётся пользователем)
- `T` — температура в Кельвинах (273 + °C из .log)
- `E_a = 7.850731×10⁻²⁰` Дж — энергия активации диффузии водорода
- `0.355×10⁹ м⁻¹` = 0.355 нм⁻¹ — параметр зависимости от поля в окисле

Разложение по физическому смыслу:

| Множитель | Смысл |
|---|---|
| `(tox·q/ε_ox)³` | куб напряжения на 1 слой оксида (E_ox³) |
| `(1×10²⁶)²` | плотность связей Si-H на интерфейсе [м⁻²]² |
| `Cox · (Vgs − Vth)` | заряд инверсионного слоя |
| `exp(−E_a/kT)^0.5` | термическая активация диссоциации Si-H |
| `exp(2·ΔV·0.355nm⁻¹)` | зависимость скорости диффузии от поля |

### 3.6 Сдвиг порогового напряжения ΔVth (NBTI)

Модель реакция-диффузия (R-D model), степенной закон по времени:

```
ΔVth_NBTI = 0.7 · (Kv · sqrt(ratio · 2 · t_life))^(2·n)
```

Где:
- `n = 1/6` — показатель степени временно́й зависимости R-D модели
- `2·n = 1/3` — итоговый показатель (∛ от выражения)
- `0.7` — эмпирический коэффициент калибровки
- `ratio · 2` — учитывает симметрию цикла нагрузки (duty cycle ~ 50%)

При `Kv · sqrt(...) ≤ 0` → `ΔVth = 0` (деградации нет).

### 3.7 Коэффициент снижения подвижности (NBTI)

```
factor_u0_NBTI = 1 + (0.24×10⁻¹⁶) · (ΔVth_NBTI · Cox / q)
```

Идентичен формуле для HCI. Применяется к параметру u0 PMOS-модели.

---

## 4. Категоризация деградации для замены модели

После расчёта ΔVth определяется, насколько изменится пороговое напряжение
относительно номинального значения:

```
ratio_vth = Vth_aged / Vth_nominal = (|Vth₀| + ΔVth) / |Vth₀|
```

На основе `ratio_vth` транзистор относится к одной из категорий:

| Категория | Диапазон ratio | Множитель vth0 | Суффикс модели |
|---|---|---|---|
| 0 (норма) | ≤ 1.000 | 1.0000 | без изменений |
| 1 | 1.000 < ratio ≤ 1.025 | 1.0125 | NMOS1 / PMOS1 |
| 2 | 1.025 < ratio ≤ 1.050 | 1.0375 | NMOS2 / PMOS2 |
| 3 | ratio > 1.050 | 1.0625 | NMOS3 / PMOS3 |

Множители vth0 выбраны как середины соответствующих диапазонов:
- Категория 1: (1.000 + 1.025) / 2 = 1.0125
- Категория 2: (1.025 + 1.050) / 2 = 1.0375
- Категория 3: ≥ 1.050 — принимается консервативная средняя оценка 1.0625

**Важно:** верхней границы у категории 3 нет. Любая деградация свыше 5 %
(включая экстремальные случаи) всегда попадает в категорию 3, а не
игнорируется. Это гарантирует, что сильно деградировавшие транзисторы
всегда получат изменённую модель.

В файле `models_aged.txt` создаётся по одному блоку `.model` на каждую
использованную категорию (NMOS1, NMOS2, NMOS3 и аналогично для PMOS).

---

## 5. Численные методы и допущения

### 5.1 Численное интегрирование

Интегрирование (∫I_hci dt, ∫|Vgs| dt) выполняется **встроенным решателем
LTSpice** через директиву `.measure tran ... integ ...`. Программа только
читает готовый результат из `.log` файла. Метод интегрирования — трапеций
(используется по умолчанию в LTSpice для `.measure`).

### 5.2 Температурная зависимость

Температура берётся из `.log` файла (строка `temp = ...` или `.temp ...`).
Используется для вычисления Kv в NBTI-расчёте:

```
T [К] = 273 + T [°C]
```

HCI-расчёт напрямую не содержит температурного множителя в `ΔVth`
(он учитывается неявно через ток I_hci в симуляции LTSpice).

### 5.3 Допущение о стационарности нагрузки

Программа предполагает, что **рабочий режим транзистора в ходе симуляции
представителен для всего срока службы**. То есть:

```
t_aging = ratio × t_life
```

где ratio = t_active / t_sim — доля «вредного» времени в симуляции.

### 5.4 Коррекция u0 по категориям деградации

Поскольку для каждой степени деградации создаётся **отдельная модель**
(NMOS1, NMOS2, NMOS3), параметр u0 корректируется **индивидуально для
каждой категории** — только по транзисторам, попавшим в эту категорию:

```
factor_u0_cat = mean(factor_u0_i  для транзисторов категории cat данного типа)
u0_aged[cat] = u0_original / factor_u0_cat
```

Пример: если три NMOS-транзистора попали в категории 1, 1, 2 —
то `u0` в NMOS1 будет понижен на среднее factor двух транзисторов категории 1,
а `u0` в NMOS2 — на factor единственного транзистора категории 2.

Если в какую-либо категорию не попало ни одного транзистора — эта модель
в `models_aged.txt` не создаётся вовсе.

### 5.5 Параметр длины канала в HCI B-источнике

```
I_hci ~ (1/L_nm)^2.5
```

Масштабирование по L позволяет использовать единую формулу для транзисторов
с разными длинами канала. При `L_nm → 0` формула вырождается — транзисторы
с `L = 0` исключаются из генерации `.inc` файла.

---

## 6. Итоговая сводка формул

### HCI (NMOS)

```
Шаг 1. I_hci(t) ← симуляция LTSpice

Шаг 2. t_hci  = ∫I_hci dt  /  (max I_hci × 0.75)
        ratio  = t_hci  / t_sim

Шаг 3. ΔVth   = 1.3 × 0.22 × |Id_max/W × (I_hci_max/Id_max)² × t_life × ratio|^0.27

Шаг 4. factor = 1 + 0.24×10⁻¹⁶ × ΔVth × Cox / q
        u0_aged = u0 / factor
```

### NBTI (PMOS)

```
Шаг 1. Vgs(t), Vbs(t) ← симуляция LTSpice

Шаг 2. t_nbti = ∫|Vgs| dt  /  max(Vgs × Vbs)
        ratio  = t_nbti / t_sim

Шаг 3. Kv     = (tox·q/ε_ox)³ × (10²⁶)² × Cox × (|Vgs|−Vth)
                × √(exp(−7.85×10⁻²⁰/(k_B·T)) / 10¹⁰)
                × exp(2·(|Vgs|−Vth) / (tox × 0.355×10⁹))

        ΔVth   = 0.7 × (Kv × √(ratio × 2 × t_life))^(1/3)

Шаг 4. factor = 1 + 0.24×10⁻¹⁶ × ΔVth × Cox / q
        u0_aged = u0 / factor
```

---

## 7. Ссылки на реализацию

| Формула | Файл | Функция |
|---|---|---|
| B-источник HCI | `core/netlist_parser.py` | `generate_inc_lines()` |
| Kv для NBTI | `core/aging.py` | `_nbti_kv()` |
| ΔVth NBTI | `core/aging.py` | `calculate_nbti()` |
| ΔVth HCI | `core/aging.py` | `calculate_hci()` |
| Категоризация | `core/aging.py` | `vth_category()` |
| Замена vth0/u0 | `core/netlist_writer.py` | `write_aged_netlist()` |
