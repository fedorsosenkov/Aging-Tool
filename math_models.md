# Математические модели деградации МОП-транзисторов

Документ описывает все расчётные формулы, физические константы, допущения и
численные методы, применяемые в программе MOS Aging Analyzer.

---

## 1. Физические константы

| Обозначение | Значение | Единица | Смысл |
|---|---|---|---|
| k_B | 1.380649 × 10⁻²³ | Дж/К | Постоянная Больцмана |
| q | 1.60219 × 10⁻¹⁹ | Кл | Заряд электрона |
| ε₀ | 8.85 × 10⁻¹² | Ф/м | Диэлектрическая проницаемость вакуума |
| ε_SiO2 | 3.9 | — | Относительная проницаемость SiO₂ |
| t_year | 31 536 000 | с | Секунд в году |

Производные:

```
ε_ox = ε_SiO2 · ε₀  =  3.9 · 8.85×10⁻¹²  ≈  3.45×10⁻¹¹  Ф/м

Cox = ε_ox / tox                     [Ф/м²]  ёмкость окисла
```

---

## 2. Эффект горячих носителей (HCI) — NMOS-транзисторы

### 2.1 Физическая природа

При высоких полях вблизи стока электроны набирают достаточно энергии, чтобы
преодолеть барьер Si/SiO₂ и захватиться в окисел затвора. Это приводит к
сдвигу порогового напряжения Vth вверх и снижению подвижности носителей u0.

Интенсивность деградации определяется:
- пиковым значением тока стока Id,
- отношением тока горячих носителей к полному току (I_hci/Id),
- шириной канала W,
- временем работы в режиме переключения.

### 2.2 Ток горячих носителей (B-источник в .inc файле)

В LTSpice добавляется управляемый источник тока, аппроксимирующий поток
горячих носителей:

```
I_hci = 1×10⁻⁵ · Id(M) · (1/L_nm)^2.5
             · exp(5.7 · (V_D − V_S))
             · exp(−1.21 · (V_D − V_G))
```

Где:
- `L_nm` — длина канала в нанометрах (например, 100 для L=100нм)
- `V_D, V_S, V_G` — потенциалы стока, истока, затвора
- Множитель `(1/L_nm)^2.5` — масштабирование по длине канала
- `exp(5.7·(V_D−V_S))` — зависимость от поля вдоль канала
- `exp(−1.21·(V_D−V_G))` — зависимость от поля в окисле

LTSpice директивы, добавляемые в .inc для транзистора Mx с номером `num`:
```spice
BnumIb 0 NnumIb I=<формула выше>
RnumIb NnumIb 0 1
.measure tran HCI_Mx    integ I(BnumIb)
.measure tran maxHCI_Mx max   I(BnumIb)
.measure tran maxId_Mx  max   Id(Mx)
```

### 2.3 Извлечение измерений из Spice Error Log

После симуляции из `.log` файла берутся три значения:

| Переменная | Ключ в .log | Смысл |
|---|---|---|
| `integ_val` | `HCI_Mx` | ∫ I_hci dt  [Кл] |
| `max_hci` | `maxHCI_Mx` | max I_hci  [А] |
| `max_id` | `maxId_Mx` | max Id  [А] |

### 2.4 Время работы в режиме HCI

```
t_hci = |integ_val / (max_hci × 0.75)|      [с]
```

Коэффициент 0.75 — инженерное допущение: считается, что полная мощность
горячих носителей действует только 75 % времени переключения (учёт
неидеальности прямоугольника тока).

```
ratio_hci = |integ_val / (max_hci × 0.75 × t_sim)|   [безразм.]
```

Где `t_sim` — полное время симуляции (FROM…TO в .log).

### 2.5 Сдвиг порогового напряжения ΔVth (HCI)

Модель деградации по степенному закону:

```
ΔVth_HCI = 1.3 · C₂₂ · |Id_max/W · (max_hci/Id_max)^m · t_life · ratio|^n
```

Параметры модели:

| Параметр | Значение | Смысл |
|---|---|---|
| C₂₂ | 0.22 | эмпирический коэффициент калибровки |
| m | 2 | показатель степени по отношению токов |
| n | 0.27 (настраивается) | показатель степени временно́й зависимости |
| 1.3 | — | инженерный запас (margin) |
| t_life | years × 31 536 000 | требуемый ресурс [с] |

Размерный анализ аргумента:

```
[Id_max/W] = А/м
[(max_hci/Id_max)^m] = безразм.
[t_life · ratio] = с
→ аргумент: [А/м · с]  →  при n=0.27 размерность в В (эмпирически)
```

### 2.6 Концентрация интерфейсных ловушек (HCI)

```
N_it = ΔVth_HCI · Cox / q = ΔVth_HCI · ε_ox / (tox · q)      [м⁻²]
```

Физически: `N_it` — поверхностная плотность заряженных состояний на интерфейсе
Si/SiO₂, созданных горячими носителями. Выводится в HTML-отчёт как справочная
физическая величина. Ожидаемый порядок: 10¹³ – 10¹⁵ м⁻².

### 2.7 Коэффициент снижения подвижности (HCI)

```
factor_u0_HCI = 1 + σ · N_it
```

где `σ` — сечение захвата (по умолчанию 0.24×10⁻¹⁶ м², настраивается).
Значение σ = 5×10⁻¹⁶ м² используется для техпроцесса 28 нм (Ma et al., 2013).

Параметр u0 модели в netlist делится на `factor_u0`:

```
u0_aged = u0_original / factor_u0
```

---

## 3. Температурная нестабильность при отрицательном смещении (NBTI) — PMOS-транзисторы

### 3.1 Физическая природа

При отрицательном смещении затвора (режим инверсии PMOS) и повышенной
температуре ионы водорода из интерфейса Si/SiO₂ диффундируют в окисел.
Это создаёт положительный заряд, сдвигающий Vth в сторону более отрицательных
значений (|Vth| увеличивается) и снижающий подвижность дырок.

### 3.2 Директивы измерений в .inc файле

```spice
.measure tran maxNBTIMx max ((V(S)-V(G))*(V(D)-V(G)))
.measure tran NBTIMx    integ ABS(V(S)-V(G))
```

Где `V(D), V(G), V(S)` — потенциалы стока, затвора, истока.

### 3.3 Извлечение измерений

| Переменная | Ключ в .log | Смысл |
|---|---|---|
| `integ_val` | `NBTIMx` | ∫\|Vsg\| dt = ∫\|Vgs\| dt  [В·с] |
| `max_val` | `maxNBTIMx` | max(Vsg · Vdg)  [В²] |

### 3.4 Время работы в режиме NBTI

```
t_nbti = integ_val / max_val      [с]
ratio_nbti = t_nbti / t_sim       [безразм.]
```

Физический смысл: `integ / max` — это «эффективное время», в течение которого
транзистор работает при максимальных напряжениях, вызывающих NBTI.

### 3.5 Коэффициент Kv

Kv характеризует интенсивность генерации ловушек в зависимости от
электрического поля в окисле и температуры:

```
Kv = (tox · q / ε_ox)³
   · (1×10²⁶)²
   · Cox
   · (|Vgs_max| − Vth)
   · (exp(−E_a / (k_B · T)) / 10¹⁰)^0.5
   · exp(2 · (|Vgs_max| − Vth) / (tox · 0.355×10⁹))
```

Где:
- `Vgs_max = sqrt(max_val)` — эффективное максимальное напряжение затвор-исток
- `Vth` — пороговое напряжение PMOS-модели (задаётся пользователем)
- `T` — температура в Кельвинах (273 + °C из .log)
- `E_a = 7.850731×10⁻²⁰` Дж — энергия активации диффузии водорода
- `0.355×10⁹ м⁻¹` = 0.355 нм⁻¹ — параметр зависимости от поля в окисле

Разложение по физическому смыслу:

| Множитель | Смысл |
|---|---|
| `(tox·q/ε_ox)³` | куб напряжения на 1 слой оксида (E_ox³) |
| `(1×10²⁶)²` | плотность связей Si-H на интерфейсе [м⁻²]² |
| `Cox · (Vgs − Vth)` | заряд инверсионного слоя |
| `exp(−E_a/kT)^0.5` | термическая активация диссоциации Si-H |
| `exp(2·ΔV·0.355nm⁻¹)` | зависимость скорости диффузии от поля |

### 3.6 Сдвиг порогового напряжения ΔVth (NBTI)

Модель реакция-диффузия (R-D model), степенной закон по времени:

```
ΔVth_NBTI = 0.7 · (Kv · sqrt(ratio · 2 · t_life))^(2·n)
```

Где:
- `n = 1/6` — показатель степени временно́й зависимости R-D модели
- `2·n = 1/3` — итоговый показатель (∛ от выражения)
- `0.7` — эмпирический коэффициент калибровки
- `ratio · 2` — учитывает симметрию цикла нагрузки (duty cycle ~ 50%)

При `Kv · sqrt(...) ≤ 0` → `ΔVth = 0` (деградации нет).

### 3.7 Концентрация интерфейсных ловушек (NBTI)

```
N_it = ΔVth_NBTI · Cox / q = ΔVth_NBTI · ε_ox / (tox · q)     [м⁻²]
```

Та же формула, что для HCI. Выводится в HTML-отчёт отдельной колонкой.

### 3.8 Коэффициент снижения подвижности (NBTI)

```
factor_u0_NBTI = 1 + σ · N_it
```

Идентичен формуле для HCI. Применяется к параметру u0 PMOS-модели.

---

## 4. Индивидуальные постаревшие модели транзисторов

Для каждого транзистора с ненулевой деградацией создаётся **отдельная**
SPICE-модель с точными вычисленными параметрами. Никакого усреднения или
квантования нет.

### 4.1 Имя модели

```
Имя постаревшей модели = {ИМЯ_ТРАНЗИСТОРА}_AGED
```

Примеры: `M1_AGED`, `M3_AGED`, `MP2_AGED`.

Транзисторы без деградации (ΔVth = 0) оставляют исходное имя модели.

### 4.2 Корректировка vth0

```
vth_multiplier = (Vth_ref + ΔVth) / Vth_ref       (Vth_ref = 0.4 В)
vth0_aged = vth0_original × vth_multiplier
```

Для каждого транзистора `vth_multiplier` вычисляется индивидуально на основе
точного ΔVth из расчёта. `Vth_ref = 0.4 В` — опорное значение для масштабирования
(применяется, если vth0 из модели совпадает с типичным).

### 4.3 Корректировка u0

```
u0_aged = u0_original / mobility_factor
```

Где `mobility_factor = 1 + σ · N_it` — точное значение из расчёта для данного
транзистора (не усреднённое).

### 4.4 Структура models_aged.txt

Для каждой базовой модели (NMOS, PMOS, CMOSN, CMOSP) сначала записывается
оригинальный блок `.model` без изменений (для транзисторов без деградации),
затем — по одному блоку на каждый деградировавший транзистор:

```spice
.model CMOSN NMOS (vth0=0.400 u0=270.0 ...)   ← оригинал, без изменений
.model M1_AGED NMOS (vth0=0.412 u0=268.3 ...)  ← индивидуальная модель M1
.model M3_AGED NMOS (vth0=0.423 u0=265.1 ...)  ← индивидуальная модель M3
.model CMOSP PMOS (vth0=-0.400 u0=90.0 ...)    ← оригинал PMOS
.model MP2_AGED PMOS (vth0=-0.419 u0=88.7 ...) ← индивидуальная модель MP2
```

---

## 5. Численные методы и допущения

### 5.1 Численное интегрирование

Интегрирование (∫I_hci dt, ∫|Vgs| dt) выполняется **встроенным решателем
LTSpice** через директиву `.measure tran ... integ ...`. Программа только
читает готовый результат из `.log` файла. Метод интегрирования — трапеций
(используется по умолчанию в LTSpice для `.measure`).

### 5.2 Температурная зависимость

Температура берётся из `.log` файла (строка `temp = ...` или `.temp ...`).
Используется для вычисления Kv в NBTI-расчёте:

```
T [К] = 273 + T [°C]
```

HCI-расчёт напрямую не содержит температурного множителя в `ΔVth`
(он учитывается неявно через ток I_hci в симуляции LTSpice).

### 5.3 Допущение о стационарности нагрузки

Программа предполагает, что **рабочий режим транзистора в ходе симуляции
представителен для всего срока службы**. То есть:

```
t_aging = ratio × t_life
```

где ratio = t_active / t_sim — доля «вредного» времени в симуляции.

### 5.4 Индивидуальная коррекция u0

Поскольку каждый деградировавший транзистор получает собственную модель,
параметр u0 корректируется **точным** значением `mobility_factor` этого
конкретного транзистора:

```
u0_aged[Mx] = u0_original / mobility_factor[Mx]
```

Никакого усреднения нет. Два транзистора с разными ΔVth получат разные u0
даже при одинаковом исходном u0 в модели.

### 5.5 Параметр длины канала в HCI B-источнике

```
I_hci ~ (1/L_nm)^2.5
```

Масштабирование по L позволяет использовать единую формулу для транзисторов
с разными длинами канала. При `L_nm → 0` формула вырождается — транзисторы
с `L = 0` исключаются из генерации `.inc` файла.

---

## 6. Итоговая сводка формул

### HCI (NMOS)

```
Шаг 1. I_hci(t) ← симуляция LTSpice

Шаг 2. t_hci  = ∫I_hci dt  /  (max I_hci × 0.75)
        ratio  = t_hci  / t_sim

Шаг 3. ΔVth   = 1.3 × 0.22 × |Id_max/W × (I_hci_max/Id_max)² × t_life × ratio|^n
         n = 0.27 по умолчанию (настраивается)

Шаг 4. N_it   = ΔVth × ε_ox / (tox × q)         [м⁻²]
        factor = 1 + σ × N_it
        u0_aged = u0 / factor
         σ = 0.24×10⁻¹⁶ м² по умолчанию (настраивается)
```

### NBTI (PMOS)

```
Шаг 1. Vgs(t), Vbs(t) ← симуляция LTSpice

Шаг 2. t_nbti = ∫|Vgs| dt  /  max(Vgs × Vbs)
        ratio  = t_nbti / t_sim

Шаг 3. Kv     = (tox·q/ε_ox)³ × (10²⁶)² × Cox × (|Vgs|−Vth)
                × √(exp(−7.85×10⁻²⁰/(k_B·T)) / 10¹⁰)
                × exp(2·(|Vgs|−Vth) / (tox × 0.355×10⁹))

        ΔVth   = 0.7 × (Kv × √(ratio × 2 × t_life))^(1/3)

Шаг 4. N_it   = ΔVth × ε_ox / (tox × q)         [м⁻²]
        factor = 1 + σ × N_it
        u0_aged = u0 / factor
```

---

## 7. Ссылки на реализацию

| Формула | Файл | Функция |
|---|---|---|
| B-источник HCI | `core/netlist_parser.py` | `generate_inc_lines()` |
| Kv для NBTI | `core/aging.py` | `_nbti_kv()` |
| ΔVth NBTI, N_it, σ | `core/aging.py` | `calculate_nbti()` |
| ΔVth HCI, N_it, σ, n | `core/aging.py` | `calculate_hci()` |
| Категоризация | `core/aging.py` | `vth_category()` |
| Замена vth0/u0 | `core/netlist_writer.py` | `write_aged_netlist()` |
| HTML-отчёт (колонка N_it) | `core/report.py` | `_build_hci_section()`, `_build_nbti_section()` |

---

## 8. Известные ограничения модели

Раздел фиксирует принципиальные расхождения между реализацией и современными
физическими моделями. Эти ограничения задокументированы как осознанные упрощения.

### L1. Модель NBTI — принципиально другая физика

**Что реализовано:** R-D (Reaction-Diffusion) модель Alam–Mahapatra:
`ΔVth ∝ t^(1/3)` — единый механизм, ст  епенная зависимость от времени.

**Что говорит современная литература (Ma et al., 2013):** двухмеханизмная модель:
- Захват дырок в оксидные ловушки: `ΔVth_trap ∝ t^0.14`
- Генерация интерфейсных состояний: `ΔVth_int ∝ log(1 + t/τS)`

Авторы прямо указывают, что R-D модель «recently challenged due to its
insufficient accuracy» (§I). Логарифмическая зависимость точнее воспроизводит
данные при малых временах (<10³ с).

**Следствие для текущей программы:** NBTI-деградация может быть как завышена,
так и занижена в зависимости от временно́го диапазона. При t >> τS R-D модель
растёт быстрее.

### L2. NBTI — восстановление (recovery) не учитывается

**Что реализовано:** только накопление деградации; ΔVth монотонно растёт.

**Реальность:** после снятия стресса (Vgs → 0) часть ΔVth быстро восстанавливается
(«fast recovery», Ma et al. Eq. 6, 15). Характерное время — миллисекунды–секунды.

**Следствие:** программа даёт **пессимистичный** результат (верхняя оценка
деградации). Для инженерного расчёта запаса надёжности это допустимо.

### L3. CHC в PMOS-транзисторах не моделируется

**Что реализовано:** для PMOS только NBTI; Hot Carrier Injection при ненулевом Vds
полностью игнорируется.

**Реальность (Ma et al., 2013, Eq. 17):**
```
ΔVth_CHC = C · {exp[−φit/(q·λ·Emax)] · (Ids/W) · t}^0.5
```

При `|Vds| > 0.5·|Vdd|` вклад CHC в PMOS может быть сопоставим с NBTI
(Fig. 6 у Ma et al.).

**Ограничение реализации:** Emax (максимальное боковое поле) не извлекается
стандартными директивами `.measure` в LTSpice. Потребовалась бы дополнительная
постобработка данных симуляции.

### L4. «Разворотный» эффект (turn-around effect) при Vds ≠ 0 для PMOS

**Что реализовано:** NBTI и CHC рассматриваются независимо; при ненулевом Vds
сумма не пересчитывается.

**Реальность:** при ненулевом Vds в PMOS вертикальное поле (ответственное за NBTI)
ослабляется, а боковое поле (ответственное за CHC) усиливается. Суммарный ΔVth
сначала *уменьшается* с ростом |Vds|, затем снова растёт — немонотонная функция
(«turn-around», Ma et al. Fig. 5).

**Следствие:** деградация PMOS при высоких |Vds| может быть недооценена.

### L5. Насыщение деградации при больших временах

**Что реализовано:** степенная модель `ΔVth ∝ t^n` неограниченно растёт.

**Реальность (Wang et al., 2022, Eq. 14):**
```
ΔVth = V0 · [1 − exp(−(t/τ)^m)]   → насыщается при t >> τ
```

При `t >> τ` деградация выходит на плато. Степенная модель в этой области
переоценивает деградацию.

**Практическое следствие:** для расчётов на стандартный ресурс (10 лет ≈ 3×10⁸ с)
переоценка обычно невелика; для t >> t_life (экстраполяция за пределы ресурса)
результаты ненадёжны.

### L6. Температурная зависимость HCI (наноразмерные устройства)

**Что реализовано:** температура не входит явно в формулу ΔVth_HCI; она
учитывается только неявно через ток Id в симуляции LTSpice.

**Реальность (Wang et al., 2022, §2):** в наноразмерных MOSFET (L < 100 нм)
доминирует механизм MVE (multi-vibrational excitation), при котором
`ΔVth ∝ exp(+Ea/kT)` — **более высокая температура ухудшает HCI**.
Это противоположно поведению длинноканальных транзисторов (L >> 100 нм),
где `ΔVth ∝ exp(−Ea/kT)`.

Параметр `Ea/k ≈ 696.5 K` — для длинноканальных устройств из расчётного листа
(Mathcad); для коротких транзисторов знак и величина другие.
